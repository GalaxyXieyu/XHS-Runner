# Packaging

## Electron Builder
Use the scripts in package.json:
- npm run pack
- npm run dist
- npm run dist:mac
- npm run dist:win

## Auto-Update Strategy
Update channel is stored in config.json (updateChannel). Hook the updater to your release channel as needed.
