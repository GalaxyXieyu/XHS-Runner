# ============================================
# XHS Generator 环境配置
# ============================================

# ------------------------------------------
# 数据库配置（必填）
# ------------------------------------------
# 本地开发 - Docker PostgreSQL
DATABASE_URL=postgresql://xhs_admin:xhs_dev_password@localhost:5432/xhs_generator

# 或者使用你已有的 PostgreSQL 容器：
# DATABASE_URL=postgresql://your_user:your_password@localhost:5432/your_database

# ------------------------------------------
# Supabase 导出配置（仅用于导出）
# ------------------------------------------
# SUPABASE_DB_URL=postgresql://postgres:your_password@db.xxxxx.supabase.co:5432/postgres

# ------------------------------------------
# Langfuse 配置（可选，用于 Prompt 管理）
# ------------------------------------------
LANGFUSE_SECRET_KEY=
LANGFUSE_PUBLIC_KEY=
LANGFUSE_BASE_URL=http://localhost:23022

# ------------------------------------------
# API 配置
# ------------------------------------------
NEXT_PUBLIC_API_URL=http://localhost:33001

# ------------------------------------------
# 对象存储配置（可选）
# ------------------------------------------
# 存储类型：local（本地文件系统）或 minio（MinIO 对象存储）
STORAGE_TYPE=local

# MinIO 配置（当 STORAGE_TYPE=minio 时需要）
MINIO_ENDPOINT=localhost
MINIO_PORT=23030
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=xhs-assets
MINIO_USE_SSL=false
MINIO_REGION=us-east-1
